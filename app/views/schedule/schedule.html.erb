<% app/views/schedules/schedule.html.erb %>
<h1>週間スケジュール</h1>
# 現在の進捗　viewを作成したが、pathを作成できていない、またウェブのほうに反映されない不具合あり
<% --- ナビゲーション --- %>
  <% `schedules_path`はご自身のルート設定に合わせてください %>
  <%= link_to '<< 先週', schedules_path(start_date: @date - 1.week), class: "btn btn-primary" %>
  <%= link_to '今週', schedules_path, class: "btn btn-secondary" %>
  <%= link_to '次週 >>', schedules_path(start_date: @date + 1.week), class: "btn btn-primary" %>
</nav>

<% --- 週カレンダー本体 --- %>
<div class="weekly-calendar">
  <table class="table table-bordered">
    <thead>
      <tr>
        <% @week_days.each do |day| %>
          <%# 日本語の曜日にする場合: %w(月 火 水 木 金 土 日)[day.wday-1] %>
          <th><%= day.strftime("%a") %><br><%= day.day %></th>
        <% end %>
      </tr>
    </thead>
    <tbody>
      <tr>
        <% @week_days.each do |day| %>
          <%# 今日の日付をハイライトするためのCSSクラス %>
          <td class="<%= 'today' if day == Date.today %>">
            <%# ここにその日のスケジュールを表示するロジックを記述 %>
            <%# 例:
              @schedules.each do |schedule|
                if schedule.starts_at.to_date == day
                  <p><%= schedule.name %></p>
                end
              end
            %>
          </td>
        <% end %>
      </tr>
    </tbody>
  </table>
</div>